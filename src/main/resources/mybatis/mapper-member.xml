<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.atguigu.atcrowdfunding.portal.dao.MemberDao">
	
	<select id="queryCertsByAccttype" resultType="cert">
		select 
			*
			from t_cert
			where id in (
				select 
				certid
				from t_account_type_cert
				where accttype = #{accttype}
			)
	</select>
	
	<select id="getAuthcode" resultType="member">
		select authcode from t_member where id = #{id}
	</select>
	
	<select id="queryTicket" resultType="ticket">
		select * from t_ticket where memberid = #{id} and status = '0'
	</select>
	
	
	<select id="queryLoginMember" resultType="member">
		select 
			*
			from t_member
			where loginacct = #{loginacct} and memberpswd = #{memberpswd}
	</select>
	
	<update id="updateAccttype">
		update t_member
		set accttype = #{accttype}
		where id = #{id}
	</update>
	
	<insert id="insertMemberCerts">
		insert into t_member_cert (
			memberid,certid,iconpath
		) values
			<foreach collection="mcs" item="mc" separator=",">
			( #{mc.memberid}, #{mc.certid}, #{mc.iconpath} )
			</foreach>
	</insert>
	
	<insert id="insertTicket">
		insert into t_ticket (
			memberid,status
		) values (
			#{memberid},#{status}
		)
	</insert>
	
	<update id="updateBasicInfo">
		update t_member
		set 
		realname = #{realname},
		cardnum = #{cardnum},
		tel = #{tel}
		where id = #{id}
	</update>
	
	<update id="updateEmail">
		update t_member
		set email = #{email}
		where id = #{id}
	</update>
	
	<update id="updateTicketProcessStep">
		update t_ticket
		set pstep = #{pstep}
		where id = #{id}
	</update>
	
	<update id="updateMemberAuthStatus">
		update t_member
		set authstatus = #{authstatus}
		where id = #{id}
	</update>
</mapper>